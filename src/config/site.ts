import { SiteConfig } from '@/types';

export const siteConfig: SiteConfig = {
  metadata: {
    name: 'timu-119',
    description: 'HTML・Node.js・JavaScript',
    url: 'https://tmyt105.leyhp.com/'
  }
}
